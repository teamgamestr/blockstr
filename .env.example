# Blockstr Environment Variables

# REQUIRED: Blockstr's private key for signing score events
# This can be either:
# - nsec format: nsec1...
# - hex format: 64-character hexadecimal string
#
# SECURITY: Never commit the actual .env file with real keys!
BLOCKSTR_NSEC=nsec1your_private_key_here_or_hex_format

# Optional: Port for the server (default: 3000)
PORT=3000

# Optional: Node environment
NODE_ENV=production
